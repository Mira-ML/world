version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22.x
    commands:
      - npm install
      - npm install -D terser
  build:
    commands:
      - npm run build --if-present

artifacts:
  files:
    - '**/*'
  base-directory: 'build'
